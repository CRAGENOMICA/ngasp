These are NetBeans files and folders: (Linux configuration only)

  ./Makefile
  ./nbproject/
  ./build/                        Object files. It can be deleted.
  ../../release/ngasp             The Netbeans' Binary file is created here



He creado el fichero makefile como una copia de Makefile.

