/**
 * ============================================================================
 *  \brief     build_backend.gradle
 *  \details   This file describes the backend binary build process
 *  \author    jjene
 *  \version   1.0
 *  \date      Apr 20, 2015
 *  \pre
 *  \bug
 *  \warning
 *  \copyright CRAG 2015.
 * ============================================================================
*/
apply plugin: 'cpp'

/*
--------------------------------------------------------------------------------
Attention:

The current working directory is this file's folder: ".gradle/"
--------------------------------------------------------------------------------
*/

/*
================================================================================
IMPORTS
================================================================================
*/

apply from: './platform.gradle'

/*
================================================================================
BUILD
================================================================================
*/
task onwBuild() {
  if (SOP == "WINDOWS") {
    apply from: './build_backend_windows.gradle'
  } else {
    if (SOP == "MAC") {
      apply from: './build_backend_mac.gradle'
    } else {
      if (SOP == "LINUX") {
        apply from: './build_backend_linux.gradle'
      } else {
      }
    }
  }
}

build.dependsOn onwBuild

clean {
    delete "./${buildDir}/"
}



